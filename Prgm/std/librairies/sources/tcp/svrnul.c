#include "environnement.h"

#include "html.h"
extern TypeSvrBox SvrLink;

/* ========================================================================== */
int HttpRepond(char *requete) { return(1); }
/* ========================================================================== */
// int HttpRepondClient(TypeHttpClient *client) { return(1); }
/* ========================================================================== */
int SvrActive(void) { return(1); }
/* ========================================================================== */
int SvrAborted(void) { return(1); }
/* ========================================================================== */
int SvrQuited(void) { return(1); }
/* ========================================================================== */
int SvrExited(void) { return(1); }
/* ========================================================================== */
int SvrUser(void) { SvrError(&SvrLink,"Serveur vide",13); return(1); }
